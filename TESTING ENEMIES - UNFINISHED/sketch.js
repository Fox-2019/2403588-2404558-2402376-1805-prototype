// Tring to take snipets of code and peice together how to get the enemies to move randomly 
// this is unfinished code and is still being tested
// so far the enemies simply reload randomly each time the sketch is refreshed


// Enemies
//let bowser;
//let goombas = [];
//let goombaSize = 80;
//let numGoombas = 5;

// enemy speed variable 
//var speedX = 2.5;
//var speedY = 1.6;
//var x;
//var y;


// sprites
//let bowserSprite;
//let goombaSprite;

// function preload(){
//  goombaSprite = loadImage("pinky.png");
//  bowserSprite = loadImage("dragon.png");
 }


//function setup() {
// createCanvas(500, 500);
// x= width/2;
// y = height/2;


// bowser = new Enemy(bowserSprite, 250, 250, 99);
 

 // FOR LOOP TO CREATE GOOMBAS - store goombas in array
// for ( let i = 0; i < 100; i++){
//   goombas[i] =  new Enemy(goombaSprite,
                       //   random(0, width),
                       //   random(0, height),
                      //    goombaSize)
 }

 
   }

//function draw(){ 
//background(245);

//x = x + random(-speedX, speedX);
//y = y + random(-speedY, speedY);
//(x, y, 20, 20);

//bowser.display();



// FOR LOOP TO DISPLAY GOOMBAS - Access instances from array created in setup()

//for ( let i = 0; i< numGoombas; i++){
//  goombas[i].display();
  

}
}


//class Enemy{// capitalise class names
 // constructor(sprite, x, y, size, points){
//    this.sprite=sprite;
//    this.x=x;
//    this.y=y;
 //   this.size = size;
  //  this.speed = 5;

    
  
  }
//  display(){
 //   image(this.sprite, this.x, this.y, this.size);
  }
}


// class move funtion to get the enemies to move
//class move {


}
